{
  "bsonType": "object",
  "required": ["rowPlacement", "colPlacement", "createdAt", "lastChange", "maxSize", "blocked", "wheels", "status", "removedAt"],
  "properties": {
    "_id": {
      "bsonType": "objectId",
      "description": "must be an objectId and is required"
    },
    "rowPlacement": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "colPlacement": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "createdAt": {
      "bsonType": "date",
      "description": "must be a date and is required"
    },
    "lastChange": {
      "bsonType": "date",
      "description": "must be a date and is required"
    },
    "lastOrder": {
      "bsonType": "objectId",
      "description": "must be an objectId"
    },
    "maxSize": {
      "bsonType": "int",
      "description": "must be an integer and is required"
    },
    "blocked": {
      "bsonType": "bool",
      "description": "must be a boolean and is required"
    },
    "wheels": {
      "bsonType": "array",
      "items": {
        "bsonType": "objectId",
        "description": "must be an objectId"
      },
      "description": "must be an array of objectIds and is required"
    },
    "status": {
      "bsonType": "string",
      "description": "must be a string and is required"
    },
    "removedAt": {
      "bsonType": "date",
      "description": "must be a date and is required"
    }
  }
}
